// These are the most basic features that the transpiler should implement in the target language. This is the best starting point to understand the transpiler creation process, and manage how ouput will be generated in the new transpiler.

let variable = 20
console.log(variable)
variable = 15
console.log('The new value of the variable is: ' + variable)

const variable2 = 20
console.log(variable + ' is the value of variable 2')
